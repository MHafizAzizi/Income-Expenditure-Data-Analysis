# Project Context — Income-Expenditure Data Analysis

> Living document. Read at session start, update at session end.

## What This Project Is

Personal income/expenditure analysis. Data from Money Manager app. Original analysis done in Excel + Power BI (see README Medium links). Codebase being rebuilt from scratch — old Streamlit scripts removed.
Stack: Python + pandas. `MasterFile.csv` is now generated, not hand-kept. Analysis/viz layer TBD.

## What Was Decided

- 2026-06-14: Scrapped old Streamlit dashboard — starting from scratch. Removed `Main-Page.py`, `pages/`.
- 2026-06-14: `data/MasterFile.csv` is GENERATED by `build_master.py` from `data/raw/` — never hand-edit. Edit raw exports + rerun instead. Reproducible, handles overlap/dedup/missing-history.
- Dedupe: raw exports overlap; `23-06-2025.csv` is date-only (degraded dup of timed xlsx rows) → dropped by date-level match; xlsx microseconds floored to seconds so timed exports dedupe exactly.

## Pending Tasks

- Decide analysis/viz layer (Streamlit, notebook, etc — none chosen yet).
- DATA QUALITY: Category/Account names drift across exports (e.g. "Food & Drink"→"🍜 Food", "Dompet"→"Cash"). Not harmonised. Clean before aggregating by category.
- Drop new Money Manager exports into `data/raw/` then rerun `python build_master.py` to refresh.

## Key Files

| File | Role |
|---|---|
| `build_master.py` | Merges `data/raw/` → `data/MasterFile.csv` (normalise, dedupe, derive Year/Month, sort) |
| `data/MasterFile.csv` | GENERATED master (3764 rows, 2022-07→2026-06). Cols: Date, Year, Month, Account, Category, Subcategory, Note, MYR, Income/Expense |
| `data/raw/` | Raw Money Manager exports (3 csv + 1 xlsx); xlsx 14-06-2026 is latest/largest |
| `requirements.txt` | pandas, openpyxl |

## Last Session — 2026-06-14

- Cloned repo; bootstrapped `context.md` + `CLAUDE.md`.
- Removed all Streamlit scripts (`Main-Page.py`, `pages/`).
- Wrote `build_master.py` to regenerate `MasterFile.csv` from raw exports. Output: 3764 rows, full 2022-07→2026-06 history, 2092 dup/degraded rows dropped from 5856 raw.
- Added `requirements.txt` (pandas, openpyxl).
