# Project Context — Income-Expenditure Data Analysis

> Living document. Read at session start, update at session end.

## What This Project Is

Personal income/expenditure analysis. Data from Money Manager app. Original analysis done in Excel + Power BI (see README Medium links). Codebase being rebuilt from scratch — old Streamlit scripts removed.
Stack: Python + pandas. `MasterFile.csv` is now generated, not hand-kept. Analysis/viz layer TBD.

## What Was Decided

- 2026-06-14: Scrapped old Streamlit dashboard — starting from scratch. Removed `Main-Page.py`, `pages/`.
- 2026-06-14: `data/MasterFile.csv` is GENERATED by `build_master.py` from `data/raw/` — never hand-edit. Edit raw exports + rerun instead. Reproducible, handles overlap/dedup/missing-history.
- Dedupe: raw exports overlap; `23-06-2025.csv` is date-only (degraded dup of timed xlsx rows) → dropped by date-level match; xlsx microseconds floored to seconds so timed exports dedupe exactly.
- 2026-06-14: Data harmonised in `build_master.py`. Emoji stripped from categories; renames via `category_map.csv` + `account_map.csv` (raw,canonical). Decided: flag (not drop) transfers/adjustments via `EntryType` col; merge `Dompet`→`Cash`; strip emoji to plain labels.

## Pending Tasks

- Decide analysis/viz layer (Streamlit, notebook, etc — none chosen yet).
- Drop new Money Manager exports into `data/raw/` then rerun `python build_master.py` to refresh. Add new drift mappings to the two map files as needed.
- Analysis must filter `EntryType == "Normal"` to exclude transfers/balance adjustments from spend/income sums.

## Key Files

| File | Role |
|---|---|
| `build_master.py` | Merges `data/raw/` → `data/MasterFile.csv` (normalise, dedupe, harmonise, derive Year/Month + EntryType, sort) |
| `category_map.csv`, `account_map.csv` | Manual raw→canonical rename tables applied by `build_master.py` |
| `data/MasterFile.csv` | GENERATED master (3756 rows, 2022-07→2026-06). Cols: Date, Year, Month, Account, Category, Subcategory, Note, MYR, Income/Expense, EntryType |
| `data/raw/` | Raw Money Manager exports (3 csv + 1 xlsx); xlsx 14-06-2026 is latest/largest |
| `requirements.txt` | pandas, openpyxl |

## Last Session — 2026-06-14

- Cloned repo; bootstrapped `context.md` + `CLAUDE.md`.
- Removed all Streamlit scripts (`Main-Page.py`, `pages/`).
- Wrote `build_master.py` to regenerate `MasterFile.csv` from raw exports. Added `requirements.txt` (pandas, openpyxl).
- Added data harmonisation: emoji strip + `category_map.csv`/`account_map.csv` renames + `EntryType` flag (Normal/Transfer/Adjustment). Output now 3756 rows; categories merged across full history, Dompet→Cash, no emoji.
- Confirmed `Credit Topup`→`Phone` rename (user verified same). Phone now 108 rows.
